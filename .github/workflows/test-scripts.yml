name: Test CI Scripts

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/scripts/**'

permissions:
  contents: read

jobs:
  test:
    name: Test Scripts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Run all tests
        run: |
          for test in .github/scripts/tests/test-*.sh; do
            echo "Running $test"
            bash "$test"
          done
